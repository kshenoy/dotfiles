# Config file for bat ()

# Set the theme to base16. This will use the colors from the base16 theme in-use
--theme="Catppuccin Frappe"

# Show line numbers, git modifications and file header (but no grid)
# --style="numbers,changes,header"
# Show just the grid and nothing else
--style="grid"

# Use italic text on the terminal (not supported on all terminals)
--italic-text=always

# Use ruby syntax for .txl files
--map-syntax "*.txl:Ruby"

# Use verilog syntax for .x and .sv* files
--map-syntax "*.x:Verilog"
--map-syntax "*.sv*:Verilog"
