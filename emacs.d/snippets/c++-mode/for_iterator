# -*- mode: snippet -*-
# name: for: iterator-based
# key: fori
# group: Looping
# --
for (auto ${2:it_begin} = std::begin(${1:var}), ${3:it_end} = std::end($1); $2 != $3; $2++) {
  `yas-selected-text`$0
}
